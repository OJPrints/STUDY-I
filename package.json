{
  "name": "lms-learning-management-system",
  "version": "1.0.0",
  "description": "A comprehensive Learning Management System with real-time features",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "lms",
    "education",
    "learning",
    "management",
    "system"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "multer": "^2.0.1",
    "react-countup": "^6.5.3",
    "react-jitsi": "^1.0.4"
  }
}
